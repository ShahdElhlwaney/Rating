


import 'package:flutter/material.dart';

import 'Widgets/start_rating_view_body.dart';

class StartRatingView extends StatelessWidget {
  const StartRatingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: StartRatingViewBody(),
    );
  }
}
