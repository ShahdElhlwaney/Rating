


import 'package:flutter/material.dart';

import 'Widgets/thanks_rating_view_body.dart';

class ThanksRatingView extends StatelessWidget {
  const ThanksRatingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold
      (
      body:ThanksRatingViewBody()
    );
  }
}
